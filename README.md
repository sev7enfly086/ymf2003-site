# YMF2003.COM 静态网站

## 部署

推送到 `main` 分支即触发自动部署到腾讯云 CVM。

## 本地预览

```bash
python3 -m http.server 8000
# 浏览器打开 http://localhost:8000
```

或者 VSCode 安装 `Live Server` 插件，右键 index.html → Open with Live Server。
